version: "3.9"

services:
  backend:
    image: ghcr.io/kumanamu/ecprice-backend:latest
    container_name: ecprice-backend
    restart: always
    env_file:
      - .env.prod
    ports:
      - "8080:8080"

  frontend:
    image: ghcr.io/kumanamu/ecprice-frontend:latest
    container_name: ecprice-frontend
    restart: always
    ports:
      - "3000:80"
